from django.conf.urls import patterns,include,url
from django.contrib import admin
from fitgene import views

# admin.autodiscover()

# urlpatterns = patterns('',
#                        url(r'^fitgene/',views.index, name='index'),
#                        url(r'^admin/',include(admiin.site.urls)),
#                        )
